CREATE TABLE reminders (id INTEGER PRIMARY KEY AUTOINCREMENT, user_ids TEXT NOT NULL, message TEXT NOT NULL DEFAULT "something", timestamp INTEGER NOT NULL, created_at INTEGER NOT NULL, channel_id INTEGER NOT NULL, message_id INTEGER NOT NULL, active BOOLEAN NOT NULL DEFAULT 1)